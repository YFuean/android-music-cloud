<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activitys.PlayMusicActivity">
    <ImageView
        android:id="@+id/iv_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@mipmap/test"/>
    <com.example.cloudmusic.views.PlayMusicView
        android:id="@+id/play_music_view"
        android:layout_width="260dp"
        android:layout_height="260dp"
        android:layout_gravity="center">
    </com.example.cloudmusic.views.PlayMusicView>

    <ImageView
        android:layout_width="@dimen/navBarTitleSize"
        android:layout_height="@dimen/navBarTitleSize"
        android:src="@mipmap/music_back"
        android:layout_margin="@dimen/marginSize"
        android:onClick="onBackClick"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_marginTop="100dp">
        <TextView
            android:id="@+id/tv_songName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="音乐名称"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:textSize="@dimen/titleSize"/>
        <TextView
            android:id="@+id/tv_author"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="作者"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:textSize="@dimen/titleSize"
            android:layout_marginTop="@dimen/marginSize"/>
    </LinearLayout>
</FrameLayout>